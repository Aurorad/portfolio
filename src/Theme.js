import { createMuiTheme } from '@material-ui/core';

export const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#fefefe',
        },
        secondary: {
            main: '#616161',
        },
        inherit: {
            main: '#cdcdcd',
        },
        dark: {
            main: '#000000',
        },
    },
});
